epochs: 200
batch: -1
imgsz: 640
device: 0
patience: 50

workers: 12
rect: true

project: "ml_data/experiments"
name: "yolov8n"
optimizer: "auto"
val: true                # val during training
save: true               # save checkpoints