epochs: 200
batch: -1
imgsz: 640
device: 0
patience: 50

workers: 8
rect: true
device: 0

name: "26s_rect"
optimizer: "auto"
val: true                # val during training
save: true               # save checkpoints